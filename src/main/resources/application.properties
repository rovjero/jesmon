welcome.message: Hello Mkyong

spring.datasource.url = jdbc:mysql://localhost:3306/jesmon
# Username and password
spring.datasource.username = root
spring.datasource.password = 1234
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
#spring.jpa.hibernate.ddl-auto = update
spring.jpa.hibernate.ddl-auto = none

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#==================================
# = Thymeleaf configurations
#==================================
spring.thymeleaf.check-template-location=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=
spring.thymeleaf.content-type=text/html
spring.thymeleaf.view-names=*.html
spring.thymeleaf.cache=false
server.contextPath=/
#===================================

spring.view.prefix=/WEB-INF/classes/jsp/
spring.view.suffix=
spring.view.view-names=*.jsp

#=
# Specify the Lucene Directory
spring.jpa.properties.hibernate.search.default.directory_provider = filesystem

# Using the filesystem DirectoryProvider you also have to specify the default
# base directory for all indexes
spring.jpa.properties.hibernate.search.default.indexBase = indexpath

spring.jpa.properties.hibernate.search.rebuildIndex = false
spring.jpa.properties.hibernate.search.default.rebuildIndex = false

spring.jpa.properties.hibernate.search.rebuildIndex = false
spring.jpa.properties.hibernate.search.default.rebuildIndex = false

hibernate.search.rebuildIndex = false
hibernate.search.default.rebuildIndex = false
hibernate.search.default.locking_strategy = none


spring.devtools.restart.exclude	=	static/**,public/**,templates/**
spring.devtools.restart.enabled	=	false

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rovejero
spring.mail.password=triturcia
spring.mail.from=rovejero@gmail.com
 
#mail properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true